PORT=3000

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=task_manager

JWT_SECRET=change_this_to_a_long_random_string
JWT_EXPIRES_IN=7d

# URL del frontend desplegado (produccion). Si no se define, el backend acepta peticiones de cualquier origen (asi es como funciona en local)
FRONTEND_URL=
